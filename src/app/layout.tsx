import type { Metadata } from 'next'
import { Playfair_Display, Inter } from 'next/font/google'
import './globals.css'

const playfair = Playfair_Display({
  subsets: ['latin'],
  variable: '--font-playfair',
  display: 'swap',
  weight: ['400', '500', '600', '700'],
})

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
  weight: ['300', '400', '500', '600'],
})

export const metadata: Metadata = {
  title: 'Business Diagnostic | MRT Consulting',
  description:
    'A structured diagnostic assessment that helps leaders uncover the root cause of underperformance — across strategy, operations, revenue, and financial performance.',
  openGraph: {
    title: 'Business Diagnostic | MRT Consulting',
    description:
      'Understand what is actually holding your business back. Built on the same diagnostic logic our consultants use in the first week of every engagement.',
    type: 'website',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${playfair.variable} ${inter.variable}`}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  )
}
